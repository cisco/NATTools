language: c
compiler:
  - gcc
  - clang
before_install:
 - sudo apt-get install check

# Change this to your needs
script: 
  - ./bootstrap.sh
  - ./configure CFLAGS=-std=c99 --with-check && make && make check


